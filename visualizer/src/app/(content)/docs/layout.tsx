export const metadata = {
  title: "Documentation",
  description: "Signal — Architecture, data sources, edge math, deployment",
};

export default function DocsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
